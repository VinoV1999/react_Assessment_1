/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["fakestoreapi.com"], // Add the domain(s) from which images will be loaded
  },
};

module.exports = nextConfig;
